# 08 — 机器人功能定义与系统架构

> 文档版本：v0.1.0 | 创建日期：2026-03-05 | 状态：草案
>
> 本文档定义 PRISM 机器人各子系统的功能划分和整体软硬件架构。

---

## 1. 子系统总览

```mermaid
flowchart TB
    subgraph PRISM["PRISM 机器人系统架构"]
        direction TB

        subgraph PERCEPT["感知子系统 Perception"]
            P1["2D/3D LiDAR"]
            P2["深度相机"]
            P3["RGB 相机"]
            P4["IMU + 编码器"]
            P5["超声波/TOF"]
            P6["水位/液位传感器"]
            P7["碰撞/力矩传感器"]
        end

        subgraph COGNITION["认知决策子系统 Cognition"]
            C1["SLAM 定位与建图"]
            C2["路径规划"]
            C3["任务规划"]
            C4["污渍检测与评估"]
            C5["人员检测"]
            C6["行为决策状态机"]
        end

        subgraph EXECUTION["执行子系统 Execution"]
            E1["底盘运动控制"]
            E2["机械臂运动控制"]
            E3["清洁模组控制"]
            E4["水路系统控制"]
            E5["消毒系统控制"]
        end

        subgraph HRI["人机交互子系统 HRI"]
            H1["触摸屏 UI"]
            H2["语音播报"]
            H3["LED 状态灯"]
            H4["急停按钮"]
            H5["远程监控"]
        end

        subgraph SELFCARE["自维护子系统 Self-Care"]
            SC1["自动充电"]
            SC2["自动补/排水"]
            SC3["工具自清洗"]
            SC4["健康自检"]
            SC5["OTA 升级"]
        end

        subgraph COMM["通信子系统 Communication"]
            CO1["Wi-Fi / 4G"]
            CO2["与云平台 MQTT"]
            CO3["与基站近场通信"]
            CO4["与电梯/闸机 API"]
        end
    end

    PERCEPT --> COGNITION
    COGNITION --> EXECUTION
    COGNITION --> HRI
    COGNITION --> SELFCARE
    COMM <--> COGNITION

    style PERCEPT fill:#e3f2fd,stroke:#1565c0
    style COGNITION fill:#fff3e0,stroke:#e65100
    style EXECUTION fill:#e8f5e9,stroke:#2e7d32
    style HRI fill:#f3e5f5,stroke:#6a1b9a
    style SELFCARE fill:#fce4ec,stroke:#b71c1c
    style COMM fill:#e0f2f1,stroke:#00695c
```

---

## 2. 感知子系统（Perception）

### 2.1 传感器融合架构

```mermaid
flowchart LR
    subgraph 主传感器["主传感器"]
        LID["2D LiDAR<br/>360°扫描<br/>建图+避障"]
        DCAM["深度相机<br/>RGB-D<br/>物体识别+3D感知"]
        RCAM["广角 RGB 相机<br/>污渍检测+质检拍照"]
    end

    subgraph 辅助传感器["辅助传感器"]
        IMU["9轴 IMU<br/>姿态估计"]
        ENC["轮编码器<br/>里程计"]
        USS["超声波 ×4<br/>近距避障"]
        BUMP["触边传感器<br/>碰撞保护"]
        CLIFF["防跌落<br/>台阶检测"]
    end

    subgraph 状态传感器["状态传感器"]
        WL["水位传感器<br/>清水/污水液位"]
        BAT["BMS<br/>电压/电流/温度"]
        ARM_ENC["关节编码器<br/>机械臂位姿"]
        ARM_FT["末端力矩<br/>接触力反馈"]
    end

    主传感器 --> FUSION["多传感器融合"]
    辅助传感器 --> FUSION
    FUSION --> MAP["环境模型"]
    FUSION --> LOC["自身定位"]
    FUSION --> OBJ["物体/人员检测"]
    状态传感器 --> STATUS["机器人状态"]

    style 主传感器 fill:#e3f2fd,stroke:#1565c0
    style 辅助传感器 fill:#fff3e0,stroke:#e65100
    style 状态传感器 fill:#e8f5e9,stroke:#2e7d32
```

### 2.2 感知功能明细

| 功能 | 传感器 | 算法 | 输出 | 频率 |
|------|--------|------|------|------|
| 定位 | LiDAR + IMU + 编码器 | AMCL / Cartographer | 机器人位姿 (x, y, θ) | 20 Hz |
| 建图 | LiDAR + 深度相机 | GMapping / Cartographer | 2D 栅格地图 + 3D 语义地图 | 建图阶段 |
| 障碍物检测 | LiDAR + 超声波 + 深度相机 | 点云聚类 + 深度图滤波 | 障碍物列表 (位置, 尺寸) | 10 Hz |
| 人员检测 | RGB 相机 + 深度相机 | YOLOv8 + 人体骨骼检测 | 人员位置 + 距离 | 15 Hz |
| 污渍检测 | RGB 相机 | 语义分割（自训练模型） | 污渍类型 + 区域 + 严重度 | 5 Hz |
| 门/隔间识别 | 深度相机 + RGB | 3D 平面检测 + 标志识别 | 门位置/状态（开/关/半开） | 5 Hz |
| 洁具识别 | RGB-D | 3D 物体检测 | 便器/台面/水龙头 3D 位姿 | 按需 |
| 清洁质量评估 | RGB 相机 | 清洁前后图像对比 | 清洁度评分 (0-100) | 按需 |
| 碰撞保护 | 触边传感器 + 力矩 | 阈值触发 | 碰撞事件 → 急停 | 实时 |
| 跌落保护 | 红外防跌落 × 4 | 距离阈值 | 台阶/坑洞 → 急停 | 实时 |

---

## 3. 认知决策子系统（Cognition）

### 3.1 导航系统

```mermaid
flowchart TB
    subgraph 导航架构["分层导航架构"]
        direction TB
        GL["全局规划器<br/>A* / Dijkstra<br/>全局最优路径"]
        LL["局部规划器<br/>DWA / TEB<br/>动态避障"]
        RC["恢复行为<br/>旋转/后退/重规划<br/>脱困策略"]
    end

    MAP["静态地图"] --> GL
    GOAL["目标点"] --> GL
    GL -->|全局路径| LL
    SCAN["实时传感器"] --> LL
    LL -->|速度指令| CHASSIS["底盘控制"]
    LL -->|规划失败| RC
    RC -->|重试| GL

    style 导航架构 fill:#e3f2fd,stroke:#1565c0
```

### 3.2 卫生间特化导航策略

```mermaid
flowchart TB
    subgraph 卫生间导航["卫生间特化策略"]
        direction TB
        N1["走廊导航<br/>标准 SLAM 导航<br/>宽度 ≥ 1.2m"]
        N2["隔间进入<br/>精准对准门框<br/>低速慢行 ≤ 0.2m/s"]
        N3["隔间内定位<br/>近距离避障<br/>超声波为主"]
        N4["隔间退出<br/>后退出门<br/>或原地旋转"]
        N5["洗手台区域<br/>横向对齐台面<br/>机械臂工作位"]
    end

    N1 -->|到达隔间门口| N2
    N2 -->|进入隔间| N3
    N3 -->|清洁完成| N4
    N4 -->|下一任务| N1
    N1 -->|到达洗手台| N5
    N5 -->|完成| N1

    style 卫生间导航 fill:#fff3e0,stroke:#e65100
```

### 3.3 任务规划

```mermaid
stateDiagram-v2
    [*] --> 待机 : 上电/充电完成

    待机 --> 接收任务 : 平台派发 / 定时触发

    接收任务 --> 前往目标 : 路径规划完成
    前往目标 --> 隔间检查 : 到达卫生间入口

    隔间检查 --> 走廊清洁 : 先清洁公共区域
    走廊清洁 --> 隔间清洁 : 逐间清洁

    state 隔间清洁 {
        [*] --> 检测人员
        检测人员 --> 等待 : 有人 → 跳过/等待
        检测人员 --> 进入隔间 : 无人
        进入隔间 --> 污渍评估
        污渍评估 --> 选择策略
        选择策略 --> 执行清洁
        执行清洁 --> 质量检查
        质量检查 --> 退出隔间 : 合格
        质量检查 --> 执行清洁 : 不合格（重试 ≤ 2次）
        退出隔间 --> [*]
    }

    隔间清洁 --> 洗手台清洁 : 所有隔间完成
    洗手台清洁 --> 消毒作业 : 台面清洁完成
    消毒作业 --> 质检上报 : 全场消毒完成

    质检上报 --> 返回基站 : 上报完成
    返回基站 --> 自维护 : 到达基站
    自维护 --> 待机 : 充电/补水/自清洗完成

    note right of 隔间清洁
        每间隔间的完整子流程
        有人时自动跳过
        下一轮再清洁
    end note
```

### 3.4 行为决策状态机

```mermaid
stateDiagram-v2
    [*] --> IDLE : 初始化完成

    IDLE --> NAVIGATE : 收到任务
    NAVIGATE --> CLEAN_FLOOR : 到达清洁区域
    NAVIGATE --> AVOID : 检测到行人
    AVOID --> NAVIGATE : 行人离开
    
    CLEAN_FLOOR --> CLEAN_FIXTURE : 地面完成
    CLEAN_FIXTURE --> DISINFECT : 洁具完成
    DISINFECT --> INSPECT : 消毒完成
    
    INSPECT --> NAVIGATE : 质检通过 → 下一区域
    INSPECT --> CLEAN_FLOOR : 质检不合格 → 返工

    NAVIGATE --> DOCK : 电量低 / 水箱满/空
    DOCK --> IDLE : 自维护完成

    NAVIGATE --> EMERGENCY : 碰撞/异常
    CLEAN_FLOOR --> EMERGENCY : 碰撞/异常
    CLEAN_FIXTURE --> EMERGENCY : 碰撞/异常
    EMERGENCY --> IDLE : 人工解除 / 自恢复

    state AVOID {
        [*] --> 减速
        减速 --> 停止 : 距离 < 1m
        停止 --> 语音提示 : 等待 > 10s
        语音提示 --> 绕行 : 仍有障碍
        停止 --> [*] : 障碍消失
        绕行 --> [*]
    }
```

---

## 4. 执行子系统（Execution）

### 4.1 底盘运动控制

```mermaid
flowchart LR
    CMD["速度指令<br/>(vx, vy, ωz)"] --> PID["PID 控制器<br/>左右轮独立"]
    PID --> DRV_L["左驱动电机<br/>带编码器"]
    PID --> DRV_R["右驱动电机<br/>带编码器"]
    DRV_L --> FB["编码器反馈"]
    DRV_R --> FB
    FB --> PID

    style CMD fill:#e3f2fd,stroke:#1565c0
```

| 参数 | 值 |
|------|------|
| 控制频率 | 50 Hz |
| 最大线速度 | 1.0 m/s |
| 最大角速度 | 1.5 rad/s |
| 清洁模式速度 | 0.2-0.4 m/s |
| 加速度限制 | ≤ 0.5 m/s² |

### 4.2 机械臂运动控制

```mermaid
flowchart TB
    subgraph 机械臂控制架构
        TARGET["目标位姿<br/>(x,y,z,rx,ry)"] --> IK["逆运动学求解"]
        IK --> TRAJ["轨迹规划<br/>五次多项式插值"]
        TRAJ --> J_CTRL["关节伺服控制<br/>位置+力矩双环"]
        J_CTRL --> MOTOR["关节电机 ×5"]
        MOTOR --> ENC_FB["编码器反馈"]
        MOTOR --> FT_FB["力矩反馈"]
        ENC_FB --> J_CTRL
        FT_FB --> J_CTRL
    end

    style 机械臂控制架构 fill:#e8f5e9,stroke:#2e7d32
```

**关键控制模式**：

| 模式 | 场景 | 控制策略 |
|------|------|---------|
| 位置控制 | 移动到预定义位置 | PID 位置环 |
| 力控/柔顺控制 | 擦拭台面、刷洗便器 | 阻抗控制 — 维持恒定接触力 |
| 示教-回放 | 学习新卫生间布局 | 拖动示教 + 轨迹记录 |
| 安全限位 | 防止碰撞 | 关节限位 + 力矩阈值急停 |

### 4.3 清洁作业编排

```mermaid
sequenceDiagram
    participant 决策层 as 决策层
    participant 底盘 as 底盘控制
    participant 臂 as 机械臂
    participant 水路 as 水路系统
    participant 清洁 as 清洁模组

    决策层->>底盘: 导航至隔间门口
    底盘-->>决策层: 到达

    决策层->>决策层: 人员检测 → 无人

    决策层->>底盘: 低速进入隔间
    底盘-->>决策层: 到达作业位

    rect rgb(232, 245, 233)
        Note over 底盘,清洁: 地面清洁阶段
        决策层->>水路: 开启喷水（地面）
        决策层->>清洁: 启动刷盘（低转速）
        决策层->>底盘: 覆盖式行走
        底盘-->>决策层: 地面覆盖完成
        决策层->>清洁: 启动吸水
        决策层->>底盘: 吸水覆盖行走
        决策层->>清洁: 停止刷盘+吸水
        决策层->>水路: 关闭喷水
    end

    rect rgb(227, 242, 253)
        Note over 臂,水路: 便器清洁阶段
        决策层->>臂: 展开到便器位置
        决策层->>臂: 安装马桶刷头
        决策层->>水路: 喷洒清洁剂（机械臂水路）
        决策层->>臂: 执行刷洗轨迹（力控模式）
        决策层->>水路: 冲洗
        决策层->>臂: 收纳
    end

    rect rgb(243, 229, 245)
        Note over 决策层,水路: 消毒阶段
        决策层->>水路: 雾化消毒喷洒
    end

    决策层->>底盘: 退出隔间
    决策层->>决策层: 拍照 → 质检评分 → 上报
```

---

## 5. 人机交互子系统（HRI）

### 5.1 交互方式

```mermaid
flowchart TB
    subgraph 对外交互["面向公众"]
        PU1["LED 灯带 — 状态可视化"]
        PU2["语音播报 — '正在清洁，请稍候'"]
        PU3["屏幕显示 — 清洁进度/二维码"]
        PU4["物理急停 — 紧急情况按停"]
    end

    subgraph 对运维["面向运维人员"]
        OP1["APP 远程监控 — 实时视频/状态"]
        OP2["APP 远程操控 — 紧急接管"]
        OP3["屏幕 UI — 本地配置/诊断"]
        OP4["拖动示教 — 学习新环境"]
    end

    subgraph 对平台["面向云平台"]
        CL1["MQTT — 状态上报/指令下发"]
        CL2["HTTP API — 地图/任务/OTA"]
        CL3["视频流 — WebRTC 远程监控"]
    end

    style 对外交互 fill:#e3f2fd,stroke:#1565c0
    style 对运维 fill:#e8f5e9,stroke:#2e7d32
    style 对平台 fill:#fff3e0,stroke:#e65100
```

### 5.2 安全交互协议

```mermaid
flowchart TB
    DETECT["检测到人员"] --> DIST{"距离判断"}
    
    DIST -->|"> 3m"| SLOW["降速至 0.5m/s<br/>蓝灯闪烁"]
    DIST -->|"1-3m"| WARN["降速至 0.2m/s<br/>语音提示<br/>'请注意，清洁机器人通行'"]
    DIST -->|"< 1m"| STOP["完全停止<br/>黄灯闪烁<br/>等待人员通过"]
    
    STOP --> TIMEOUT{"等待 > 30s?"}
    TIMEOUT -->|是| REPLAN["重新规划路径<br/>绕行"]
    TIMEOUT -->|否| CHECK["持续检测"]
    CHECK -->|"人员离开"| RESUME["恢复行驶"]
    CHECK -->|"人员仍在"| STOP

    style DETECT fill:#1565c0,color:#fff
    style STOP fill:#f57f17,color:#fff
```

---

## 6. 自维护子系统（Self-Care）

### 6.1 基站功能

```mermaid
flowchart TB
    subgraph 基站["PRISM 基站"]
        direction TB
        CHG["充电对接<br/>触点式/无线<br/>500W"]
        CW_FILL["清水补给<br/>自来水接口<br/>自动灌注"]
        DW_DRAIN["污水排放<br/>接排水管<br/>自动抽排"]
        CD_FILL["清洁剂补充<br/>大容量储罐<br/>自动注入"]
        WASH["工具清洗池<br/>刷盘/抹布冲洗"]
    end

    ROBOT["机器人"] -->|自动泊入| 基站
    基站 -->|完成维护| ROBOT

    style 基站 fill:#e3f2fd,stroke:#1565c0
```

### 6.2 自维护流程

```mermaid
flowchart LR
    DOCK["泊入基站"] --> CHG["开始充电"]
    DOCK --> DRAIN["排放污水"]
    DRAIN --> FILL["补充清水"]
    FILL --> CD["补充清洁剂"]
    DOCK --> TOOL_WASH["清洁工具自清洗"]
    
    CHG & CD & TOOL_WASH --> DIAG["自检诊断"]
    DIAG --> REPORT["状态上报云平台"]
    REPORT --> READY["就绪待命"]

    style DOCK fill:#1565c0,color:#fff
    style READY fill:#2e7d32,color:#fff
```

### 6.3 健康自检项目

| 检查项 | 检查方式 | 频率 | 异常处理 |
|--------|---------|------|---------|
| 电池健康 | BMS 数据分析 | 每次充电 | SOH < 80% 告警 |
| 刷盘磨损 | 电流异常检测 | 每次作业 | 磨损严重 → 提醒更换 |
| 吸水电机 | 真空度检测 | 每次作业 | 真空度下降 → 检查堵塞 |
| LiDAR 状态 | 自检信号 | 开机 | 异常 → 降级到超声波模式 |
| 相机状态 | 测试图像 | 开机 | 镜头污染 → 提醒擦拭 |
| 机械臂关节 | 零位标定偏差 | 开机 | 偏差过大 → 重新标定 |
| 网络连接 | Ping 测试 | 持续 | 断网 → 离线模式运行 |
| 水路管路 | 流量/压力 | 每次作业 | 堵塞/泄漏 → 告警 |

---

## 7. 软件架构

### 7.1 机器人端软件分层

```mermaid
flowchart TB
    subgraph APP["应用层"]
        A1["任务管理器"]
        A2["清洁策略引擎"]
        A3["人机交互管理"]
        A4["远程通信代理"]
        A5["自维护管理"]
        A6["日志与诊断"]
    end

    subgraph MID["中间件层"]
        M1["ROS 2 通信框架<br/>DDS"]
        M2["TF2 坐标变换"]
        M3["Nav2 导航栈"]
        M4["MoveIt 2 机械臂规划"]
        M5["行为树 BehaviorTree.CPP"]
    end

    subgraph DRV["驱动层"]
        D1["LiDAR 驱动"]
        D2["相机驱动"]
        D3["电机驱动<br/>CAN/EtherCAT"]
        D4["传感器驱动<br/>I²C/SPI/UART"]
        D5["水路执行器驱动"]
    end

    subgraph HW["硬件抽象层"]
        H1["MCU 固件<br/>底盘/电源管理"]
        H2["GPU 加速<br/>CUDA/TensorRT"]
    end

    APP --> MID --> DRV --> HW

    style APP fill:#e3f2fd,stroke:#1565c0
    style MID fill:#fff3e0,stroke:#e65100
    style DRV fill:#e8f5e9,stroke:#2e7d32
    style HW fill:#fce4ec,stroke:#b71c1c
```

### 7.2 ROS 2 节点拓扑

```mermaid
flowchart TB
    subgraph 感知节点
        N_LID["/lidar_driver"]
        N_CAM["/camera_driver"]
        N_IMU["/imu_driver"]
        N_USS["/ultrasonic_driver"]
    end

    subgraph SLAM节点
        N_SLAM["/slam_node<br/>Cartographer"]
        N_LOC["/localization<br/>AMCL"]
    end

    subgraph 导航节点
        N_NAV["/nav2_stack"]
        N_GP["/global_planner"]
        N_LP["/local_planner"]
        N_RC["/recovery"]
    end

    subgraph AI节点
        N_DET["/person_detector<br/>YOLOv8"]
        N_STN["/stain_detector"]
        N_QC["/quality_checker"]
    end

    subgraph 控制节点
        N_BASE["/base_controller"]
        N_ARM["/arm_controller<br/>MoveIt2"]
        N_CLEAN["/clean_module"]
        N_WATER["/water_system"]
    end

    subgraph 管理节点
        N_TASK["/task_manager<br/>BehaviorTree"]
        N_HRI["/hri_manager"]
        N_COMM["/cloud_bridge<br/>MQTT"]
        N_DIAG["/diagnostics"]
    end

    N_LID -->|/scan| N_SLAM & N_NAV
    N_CAM -->|/image, /depth| N_DET & N_STN & N_QC
    N_IMU -->|/imu| N_SLAM
    N_SLAM -->|/map| N_NAV
    N_LOC -->|/pose| N_NAV & N_TASK
    N_NAV -->|/cmd_vel| N_BASE
    N_TASK -->|目标| N_NAV & N_ARM & N_CLEAN & N_WATER
    N_DET -->|/persons| N_TASK
    N_STN -->|/stains| N_TASK
    N_COMM <-->|MQTT| CLOUD["云平台"]
    N_DIAG -->|/diagnostics| N_COMM

    style 感知节点 fill:#e3f2fd,stroke:#1565c0
    style SLAM节点 fill:#e3f2fd,stroke:#1565c0
    style 导航节点 fill:#fff3e0,stroke:#e65100
    style AI节点 fill:#f3e5f5,stroke:#6a1b9a
    style 控制节点 fill:#e8f5e9,stroke:#2e7d32
    style 管理节点 fill:#fce4ec,stroke:#b71c1c
```

---

## 8. 机器人与云平台交互

```mermaid
sequenceDiagram
    participant CLD as 云平台
    participant RBT as 机器人
    participant BST as 基站

    Note over CLD,BST: 日常作业流程

    CLD->>RBT: 下发清洁任务 (MQTT)
    RBT->>RBT: 任务解析 → 路径规划
    RBT->>CLD: 上报：任务已接收，开始执行

    loop 逐区域清洁
        RBT->>RBT: 导航 → 清洁 → 质检
        RBT->>CLD: 上报：区域完成 + 前后对比照 + 评分
    end

    RBT->>CLD: 上报：任务全部完成
    CLD->>CLD: AI 复核 + 数据入库

    RBT->>BST: 泊入基站
    BST->>RBT: 充电 + 补水 + 排污
    RBT->>CLD: 上报：自维护完成，进入待机

    Note over CLD,BST: 异常处理流程
    RBT->>CLD: 上报：异常（碰撞/卡死/传感器故障）
    CLD->>CLD: 触发告警 → 通知运维
    CLD->>RBT: 下发指令（远程接管/返回基站/急停）
```

---

> 上一篇：[07-机器人形态探究](07-机器人形态探究.md) | 下一篇：[09-机器人技术选型](09-机器人技术选型.md)
